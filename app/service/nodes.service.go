package service

/*
func GetNodes(c *fiber.Ctx) error {

	nodes := make([]MQTTNode, 0, len(connector.Topics))
	for k, v := range connector.Topics {
		nodes = append(nodes, MQTTNode{FriendlyNamesDe: k, WorkerID: v})
	}
	sort.Sort(MQTTNodeSlice(nodes))
	return c.JSON(nodes)
}
*/
